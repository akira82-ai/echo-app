# Echo 白天主题设计规范

版本：v1
来源：`design-plan.html` 05 · UI DESIGN

## 1. 目标

- 保持与夜间主题一致的结构和信息层级
- 整体更明亮、更轻、更高雅
- 体现 macOS 原生感，但不做普通白底表单
- 重点是“高级白”，不是“纯白”

## 2. 版式不变量

- 面板宽度：`560px`
- 面板高度：`425px`
- 搜索区高度：`72px`
- 列表行高：`56px`
- 底栏高度：`48px`
- 列表固定 `5` 行槽位
- 标题栏仍居中显示 `Echo`
- 不改变交互逻辑

## 3. 色板

| Token | 值 | 用途 |
| --- | --- | --- |
| `bg` | `#f6f7f9` | 页面背景 |
| `bg-soft` | `#ffffff` | 轻层背景 |
| `panel` | `rgba(255,255,255,0.82)` | 面板玻璃层 |
| `panel-solid` | `#e6e9ee` | keycap / 实底块 |
| `panel-hover` | `rgba(74,168,255,0.08)` | 悬停层 |
| `border` | `rgba(15,23,42,0.08)` | 细分割线 |
| `border-strong` | `rgba(15,23,42,0.14)` | 强边框 |
| `text` | `#1f2937` | 主文字 |
| `text-dim` | `#4b5563` | 次文字 |
| `text-faint` | `#6b7280` | 弱提示 |
| `accent` | `#3b82f6` | 选中 / 强调 |
| `accent-soft` | `rgba(59,130,246,0.10)` | 选中底色 |
| `green` | `#059669` | 成功 / 文件 |
| `purple` | `#8b5cf6` | 图片 |
| `yellow` | `#d97706` | 提示 |
| `red` | `#ef4444` | 错误 |

## 4. 组件规范

### 4.1 搜索栏

- 保持与夜间主题同一布局
- 输入框为深浅适中的灰黑字
- 提示块用浅灰底，不可偏黑
- 光标仍使用 accent 蓝

### 4.2 列表项

- 结构与夜间主题完全一致
- 选中态使用浅蓝柔光
- 缩略图底使用浅灰，不用黑底
- 类型标签使用低饱和软色

### 4.3 底栏

- 底栏背景为浅灰白
- keycap 使用 `panel-solid`
- keycap 字色使用 `#2f3642`
- 底栏整体需保持“轻”，不要压住内容

### 4.4 菜单栏与设置

- 菜单栏采用白色玻璃块
- 设置页卡片保持低对比边框
- 表单控件与按钮都要更克制

## 5. 交互状态

- Hover：浅蓝或浅灰抬亮
- Selected：浅蓝底 + 细边框
- Empty：使用浅灰提示字
- Disabled：降低饱和度，不要发灰发脏
- Error：仅局部红色提示

## 6. 动效

- 切换过渡：`0.15s - 0.3s`
- 以透明度和颜色变化为主
- 不做夸张阴影动画
- 保持整体轻盈

## 7. 禁止项

- 不要纯白到刺眼
- 不要黑色 keycap
- 不要厚重阴影
- 不要把浅色主题做成“系统默认表单”
- 不要改变夜间主题的结构骨架
