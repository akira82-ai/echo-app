# Echo 夜间主题设计规范

版本：v1
来源：`design-plan.html` 05 · UI DESIGN

## 1. 目标

- 维持 macOS 深色工具感
- 保留半透明磨砂玻璃
- 所有层级严格对齐当前 HTML UI 规范
- 重点是克制、稳、专业，不做花哨装饰

## 2. 版式不变量

- 面板宽度：`560px`
- 面板高度：`425px`
- 搜索区高度：`72px`
- 列表行高：`56px`
- 底栏高度：`48px`
- 列表固定 `5` 行槽位
- 标题栏居中显示 `Echo`
- 不强行绘制 traffic lights

## 3. 色板

| Token | 值 | 用途 |
| --- | --- | --- |
| `bg` | `#0b0d12` | 整体背景 |
| `bg-soft` | `#11141b` | 轻层背景 |
| `panel` | `rgba(28,32,42,0.72)` | 面板玻璃层 |
| `panel-solid` | `#1c202a` | keycap / 实底块 |
| `panel-hover` | `#232838` | 悬停层 |
| `border` | `rgba(255,255,255,0.08)` | 细分割线 |
| `border-strong` | `rgba(255,255,255,0.16)` | 强边框 |
| `text` | `#e8eaf0` | 主文字 |
| `text-dim` | `#9aa3b2` | 次文字 |
| `text-faint` | `#6b7385` | 弱提示 |
| `accent` | `#4aa8ff` | 选中 / 强调 |
| `accent-soft` | `rgba(74,168,255,0.14)` | 选中底色 |
| `green` | `#34d399` | 成功 / 文件 |
| `purple` | `#a78bfa` | 图片 |
| `yellow` | `#fbbf24` | 提示 |
| `red` | `#f87171` | 错误 |

## 4. 组件规范

### 4.1 搜索栏

- 左侧放放大镜图标
- 输入框字号 `18px`
- 光标使用 accent 蓝
- 右侧提示仅在有输入时显示
- 提示块使用 `panel-solid`

### 4.2 列表项

- 行内结构：编号 / 内容 / 类型标签
- 默认不压缩高度
- 选中态使用 `accent-soft + 1px accent border`
- 图片缩略图使用深灰底，不可纯黑
- 文件图标使用绿色软底

### 4.3 底栏

- 左侧为记录数状态
- 右侧为快捷键说明
- keycap 使用 `panel-solid`
- keycap 文本使用 `text-dim`
- 底栏背景略深于列表区，但不要吃死黑

### 4.4 菜单栏与设置

- 菜单栏保持深色玻璃块
- 设置页继续使用卡片式分组
- 表单行采用低对比边框

## 5. 交互状态

- Hover：轻微抬亮，不跳色
- Selected：蓝色柔光 + 细边框
- Empty：用弱提示文案，不加重视觉
- Disabled：只降对比，不降结构
- Error：红色仅用于提示，不扩大范围

## 6. 动效

- 切换过渡：`0.15s - 0.3s`
- 仅保留必要微动效
- 光标闪烁可保留
- 不使用夸张弹跳或大位移

## 7. 禁止项

- 不要纯黑 keycap
- 不要把列表背景做成实心黑块
- 不要增加额外装饰纹理
- 不要破坏当前窗口比例
- 不要引入与 HTML 规范不一致的新配色
